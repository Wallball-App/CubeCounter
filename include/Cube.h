#pragma once
#include <glm/glm.hpp>
#include <string>

using namespace glm;

class Cube {
public:
    /*Cube();
    Cube(mat4 view, mat4 projection, vec3 position);
    void SetupMatrices(mat4 view, mat4 projection, vec3 position);
    void GenerateArray();
    void bind();
    void render(mat4 &view, mat4 &projection, int count);
private:
    std::string loadShader(const char *path);
    
    // Matrix member variables
    mat4 modelMatrix;
    mat4 viewMat, projectionMat;
    
    // Shader source strings (must persist)
    std::string vertexShaderStr;
    std::string fragmentShaderStr;

    unsigned int VBO, VAO, EBO;
    unsigned int shaderProgram, vertex, fragment;

    int RandomOffset;*/
};